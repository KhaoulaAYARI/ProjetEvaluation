{% extends 'base.html.twig' %}

{% block title %}Hello FormulairesController!{% endblock %}

{% block body %}
Formulaire Update Detail Commande
{{ form_start(form, {'attr': {'id': 'updateDetailCommande'}}) }}
{{ form_row(form.quantite) }}
{{ form_row(form.prixUnitaire) }}
{{ form_row(form.produits) }}
{{ form_row(form.commandesD) }}

<input type="submit" value="Update !">
{{ form_end(form) }}
{% endblock %}


{% block javascripts %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const form = document.getElementById('updateDetailCommande');
        //id attribute on the form: Setting an id (updateDetailCommande) ensures that your JavaScript accurately selects the correct form.
        if (form) {
			//The if (form) line verifies that the form is in the DOM before trying to add the event listener.
            form.addEventListener('submit', function(event) {
                alert('Modification réussie');
				// Redirect to the new path after the alert
                window.location.href = '{{ path("afficherTousdetailsCommandes") }}';
            });
        }
    });
</script>
{% endblock %}